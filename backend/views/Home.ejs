<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>URL</h1>
    <% if (locals.id) { %>
        <p>http://localhost:8000/<%= id %></p> <!-- Changed to avoid the /url prefix -->
    <% } %>
    <div>
        <form method="POST" action="/url">
            <label for="">Enter URL</label>
            <input type="text" name="url" placeholder="http://Home.com">
            <button type="submit">Generate</button>
        </form>
    </div>
</body>
</html>
